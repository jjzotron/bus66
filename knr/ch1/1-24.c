#include <stdio.h>

/* check c program for basic syntax errors */


int main(void){
    // parentheses, brackets, and braces are balanced (can't just count, have to check for being in quotes)
    // single and double quotes are balanced -> more than
    // comments are ended
    // escape sequences?

    // cant just count stuff since it can be in quotes or comments
    return 0;
}
